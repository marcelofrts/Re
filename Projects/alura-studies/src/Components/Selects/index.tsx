import React from 'react';

class Select extends React.Component {
    render() {
        return(
            <select>
                <option disabled>Selecione a opção...</option>
            </select>
        )
    }
}

export default Select;
